export interface Gallery {
  src: string;
  title: string;
  description: string;
}

export interface ImageStyle {
  top: number;
  left: number;
  opacity: number;
  zIndex: number;
  scale: number;
}
